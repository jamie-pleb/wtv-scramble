# --- Build output (CMake build tree for obs-scramble) ---
obs-scramble/build/

# --- Local OBS SDK checkout (headers + import lib pulled in for building against libobs) ---
obs-scramble/.obs-sdk/

# --- Regenerated by obs-scramble/tools/dump_perm.c and verify-cpp-port.mjs each run; the
#     golden fixture (test/perm-1337-9.json) is what's actually checked in and compared against ---
test/perm-1337-9-cpp.json

# --- Internal meta-prompts used to have Claude build this project — not part of the public repo ---
prompts/

# --- Compiler/linker intermediates (MSVC and friends) ---
*.obj
*.pdb
*.exp
*.ilk
*.idb
*.lib
*.exe

# --- Visual Studio / MSVC tooling ---
.vs/
*.sln
*.vcxproj
*.vcxproj.filters
*.vcxproj.user

# --- Node ---
node_modules/

# --- OS cruft ---
Thumbs.db
Desktop.ini
.DS_Store

# --- Editor cruft ---
*.swp
*.swo
*~
.idea/
